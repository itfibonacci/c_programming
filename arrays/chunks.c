#include <stdio.h>

int **chunk_array(int arr[], int size, int chunk_size, int *num_chunks);

int main(){
	int my_arr[] = {2, 3, 1, 4, 1};
	printf("Size of my array is: %zu\n", sizeof(my_arr));
	return 0;
}

int **chunk_array(int arr[], int size, int chunk_size, int *num_chunks){
	
	return 0;
}
